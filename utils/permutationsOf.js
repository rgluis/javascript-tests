exports.permutationsOf = string => {
    const size = string.length;
    if (size > 50) throw 'The max length of the string is 50';

    const stringToArr = string.split('');

    for (let position = 0; position < size; position++) {

    }
};